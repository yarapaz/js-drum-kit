"use strict";const keys=document.querySelectorAll(".key");function playSound(e){const n=document.querySelector(`audio[data-key="${e.keyCode}"]`),t=document.querySelector(`.key[data-key="${e.keyCode}"]`);n&&(n.currentTime=0,n.play(),t.classList.add("playing"))}function removeTransition(e){"transform"===e.propertyName&&this.classList.remove("playing")}window.addEventListener("keydown",playSound),keys.forEach(e=>{e.addEventListener("transitionend",removeTransition)});